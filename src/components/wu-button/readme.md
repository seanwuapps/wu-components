# wu-button



<!-- Auto Generated Below -->


## Properties

| Property   | Attribute  | Description                                                               | Type                              |
| ---------- | ---------- | ------------------------------------------------------------------------- | --------------------------------- |
| `block`    | `block`    | If button will extend to full width                                       | `boolean`                         |
| `disabled` | `disabled` | If this button is disabled                                                | `boolean`                         |
| `flat`     | `flat`     | If the button will use flat style                                         | `boolean`                         |
| `href`     | `href`     | If `href` attribute is specified, the button will be output as an `a` tag | `string`                          |
| `outline`  | `outline`  | If use outline styles                                                     | `boolean`                         |
| `shape`    | `shape`    | Shape of the button                                                       | `"circle"`, `"round"`             |
| `theme`    | `theme`    | Button theme                                                              | `string`                          |
| `type`     | `type`     | Html type of the button                                                   | `"submit"`, `"reset"`, `"button"` |


## CSS Custom Properties

| Name                    | Description                            |
| ----------------------- | -------------------------------------- |
| `--wu-button-padding-x` | Horizontal padding                     |
| `--wu-button-padding-y` | Vertical padding                       |
| `--wu-button-radius`    | How rounded you want the corners to be |


----------------------------------------------

*Built with [StencilJS](https://stenciljs.com/)*
